{
  "name": "sopt-push",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w",
    "lint": "tslint '*.ts'",
    "start:dev": "NODE_ENV=dev sls invoke local --function lambda --path lib/data.json --stage dev",
    "start:prod": "NODE_ENV=prod sls invoke local --function lambda --path lib/data.json --stage prod",
    "deploy:dev": "NODE_ENV=dev sls deploy --stage dev",
    "deploy:prod": "NODE_ENV=prod sls deploy --stage prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-sdk": "0.0.42",
    "@types/node": "^7.0.5",
    "cross-env": "^7.0.3",
    "tslint": "^5.5.0",
    "tslint-config-standard": "^6.0.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.350.0",
    "@aws-sdk/client-lambda": "^3.350.0",
    "@aws-sdk/client-sns": "^3.350.0"
  }
}
